# Evidence rules

- Cite every external claim with its signal title and URL from tool output.
- Points and comment counts are momentary; phrase them as "as of right now".
- One signal may support at most one angle. Do not stack three angles on one
  story.
- If two signals conflict, surface the tension instead of picking a side.
- Never invent metrics, quotes, or URLs. A missing number stays missing.
