"use client";
import type { OutputKind } from "@/lib/types";
const OPTIONS: Array<[OutputKind, string]> = [["x_post", "X post"], ["x_thread", "Thread"], ["linkedin_post", "LinkedIn"], ["blog_article", "Article"], ["newsletter", "Newsletter"], ["social_image", "Image"], ["carousel_spec", "Carousel"], ["short_clip", "Clip"], ["reel", "Reel"], ["content_pack", "Pack"]];
export function OutputIntentSelector({ selected, onChange, disabled }: { selected: OutputKind[]; onChange: (outputs: OutputKind[]) => void; disabled?: boolean }) { return <div className="flex gap-1 overflow-x-auto" aria-label="Desired outputs">{OPTIONS.map(([id, label]) => { const active = selected.includes(id); return <button key={id} type="button" disabled={disabled} aria-pressed={active} onClick={() => onChange(active ? selected.filter((item) => item !== id) : [...selected, id])} className={`shrink-0 rounded-full border px-2 py-1 text-[8px] font-bold ${active ? "border-black bg-black text-white" : "border-black/15 bg-white/60 text-black/55"}`}>{label}</button>; })}</div>; }
