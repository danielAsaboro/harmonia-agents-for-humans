import { readFileSync } from "node:fs"; import { describe, expect, it } from "vitest";
const read = (path: string) => readFileSync(path, "utf8");
describe("resident autonomy infrastructure", () => { it("is opt-in and uses private OIDC Scheduler wakes", () => { const env = read(".env.example"), deploy = read("infra/deploy.sh"); expect(env).toContain("HARMONIA_ENABLE_RESIDENT_AUTONOMY=0"); for (const value of ["harmonia-resident-heartbeat", "/durable/heartbeat", "harmonia-resident-dream", "/durable/dream", "harmonia-resident-wakeup", "/durable/wakeup", "--oidc-service-account-email", "--oidc-token-audience", "HARMONIA_WORKSPACE_TIMEZONE", "--no-allow-unauthenticated"]) expect(deploy).toContain(value); expect(deploy).toContain('serviceAccount:harmonia-scheduler@${PROJECT_ID}.iam.gserviceaccount.com'); }); });
